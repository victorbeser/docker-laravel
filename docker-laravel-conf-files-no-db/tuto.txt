- composer create-project --prefer-dist laravel/laravel nome-do-projeto

- Dockerfile | docker-compose.yaml | .env [APP_KEY] | .docker/vhost.conf

- docker-compose build

- docker-compose up -d

- docker-compose ps

- docker-compose exec [nome] php artisan key:generate

- docker-compose exec [nome] php artisan migrate
